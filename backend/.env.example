# Example Environment Variables for Production Deployment
# Copy this file to .env and fill in the values
# NEVER commit .env to Git - it's in .gitignore

# Django Settings
DEBUG=False
SECRET_KEY=your-super-secret-key-here-generate-a-new-one
ALLOWED_HOSTS=your-domain.railway.app,localhost

# Database (Railway provides this automatically)
DATABASE_URL=postgresql://user:password@host:port/database

# CORS Settings (Frontend URL)
CORS_ALLOWED_ORIGINS=https://your-site.netlify.app,http://localhost:5500

# CSRF Settings
CSRF_TRUSTED_ORIGINS=https://your-backend.railway.app,https://your-site.netlify.app

# Instructions:
# 1. Generate a new SECRET_KEY: python -c "import secrets; print(secrets.token_urlsafe(50))"
# 2. Set DEBUG=False for production
# 3. Replace 'your-domain.railway.app' with your actual Railway URL
# 4. Replace 'your-site.netlify.app' with your actual Netlify URL
# 5. DATABASE_URL is automatically provided by Railway when you add PostgreSQL
